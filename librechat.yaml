version: 1.2.1
cache: true

endpoints:
  custom:
    - name: "PowerBlock AI Coach"
      # API key is checked by LibreChat but we ignore it in our backend
      apiKey: "powerblock"
      # Points to our FastAPI — host.docker.internal reaches the Mac host from inside Docker
      baseURL: "http://host.docker.internal:8000/v1"
      models:
        default: ["powerblock-coach"]
        fetch: false
      titleConvo: true
      titleModel: "powerblock-coach"
      summarize: false
      forcePrompt: false
      modelDisplayLabel: "PowerBlock AI"
      dropParams:
        - "stop"
        - "frequency_penalty"
        - "presence_penalty"
        - "top_p"
